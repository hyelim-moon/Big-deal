spring:
  # =========================
  # ❌ PostgreSQL datasource (주석 처리)
  # =========================
  #datasource:
  #  url: jdbc:postgresql://3.83.117.87:5432/franchise
  #  username: postgres
  #  password: qwer
  #  driver-class-name: org.postgresql.Driver

  # =========================
  # ✅ H2 datasource (PostgreSQL 없이 dev 구동)
  # =========================
  datasource:
    url: jdbc:h2:mem:devdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  mail:
    host: smtp.gmail.com
    port: 587
    username: choda1510@kyungmin.ac.kr
    password: xsdcxaxnwgyfswoi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    auth-code-expiration-millis: 1800000

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

    # ❌ PostgreSQL Dialect 주석
    #database-platform: org.hibernate.dialect.PostgreSQLDialect

    # ✅ H2 Dialect
    database-platform: org.hibernate.dialect.H2Dialect

  sql:
    init:
      mode: never
      # ❌ PostgreSQL 전용 설정 주석
      #platform: postgresql
      #data-locations: classpath:data.sql

  redis:
    host: localhost
    port: 6379

  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  expiration-time: 86400000
  secret: VGhlIGdyZWF0ZXN0IGdsb3J5IGluIGxpdmluZyBsaWVzIG5vdCBpbiBuZXZlciBmYWxsaW5nLCBidXQgaW4gcmlzaW5nIGV2ZXJ5IHRpbWUgd2UgZmFsbC4=

kakao:
  client_id: 0696c4ca8c9cf91931a3a54962777835
  redirect_uri: http://localhost:8080/kakao-login
