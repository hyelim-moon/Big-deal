spring:
  profiles:
    active: dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: never  # 변경: 데이터베이스 초기화 파일을 실행하지 않음
      platform: postgresql
      data-locations: classpath:data.sql  # 초기 데이터 파일 위치

  datasource:
    url: jdbc:postgresql://3.83.117.87:5432/franchise  # DB 연결 URL
    username: postgres  # DB 사용자 이름
    password: qwer  # DB 비밀번호
    driver-class-name: org.postgresql.Driver  # PostgreSQL 드라이버 클래스

  redis:
    host: localhost
    port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: choda1510@kyungmin.ac.kr
    password: xsdcxaxnwgyfswoi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    auth-code-expiration-millis: 1800000  # 인증 코드 만료 시간 (밀리초)

jwt:
  expiration-time: 86400000  # JWT 토큰 만료 시간 (밀리초)
  secret: VGhlIGdyZWF0ZXN0IGdsb3J5IGluIGxpdmluZyBsaWVzIG5vdCBpbiBuZXZlciBmYWxsaW5nLCBidXQgaW4gcmlzaW5nIGV2ZXJ5IHRpbWUgd2UgZmFsbC4=  # JWT 비밀 키 (Base64 인코딩)

kakao:
  client_id: 0696c4ca8c9cf91931a3a54962777835
  redirect_uri: http://localhost:8080/kakao-login  # 카카오 로그인 리다이렉트 URI

server:
  port: 8080

my:
  nickname: 8080server  # 서버의 별명 설정
